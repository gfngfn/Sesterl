
let make_sub() =
  spawn(
    receive
    | true ->
        return(())
    end
  )

letrec aux(n) =
  if n <= 0 then
    return(())
  else
    do pid <- make_sub() in
    aux(n - 1)


let main() =
  aux(10)
