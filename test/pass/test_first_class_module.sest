module TestFirstClassModule = struct
  module Sub = struct
    type t = int
    val compare(n1, n2) = n2 - n1
  end

  signature Ord = sig
    type t
    val compare : fun(t, t) -> int
  end

  val f() =
    pack Sub : Ord

  val main(args) =
    print_debug(f())

end
