package: sample_project
source_directories:
  - "./"

main_module: "SampleProject"

document_output_directory: "./_doc"

dependencies:
  - name: "stdlib"
    source:
      type: "local"
      directory: "../../../external/stdlib"

test_dependencies:
  - name: "testing"
    source:
      type: "local"
      directory: "../../../external/testing"

erlang:
  output_directory: "../../_generated"
  test_output_directory: "../../_generated_test"
