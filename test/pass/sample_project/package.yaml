package: sample_project
source_directories:
  - "./"

main_module: "SampleProject"

dependencies:
  - name: "stdlib"
    source:
      type: "local"
      directory: "../../../external/stdlib"

test_dependencies:
  - name: "testing"
    source:
      type: "local"
      directory: "../../../external/testing"
